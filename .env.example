# Configuration for GramMY Filter Bot
# You can set these environment variables directly on Koyeb/Heroku or via a .env file locally.

BOT_TOKEN=
MONGODB_URI=

# The specific MongoDB database name to use for this bot
DB_NAME=auto-filterclips

# The ID of your Database Channel where movies are forwarded
DB_CHANNEL_ID=-1002357848749

# The ID of your Group where users search for movies
GROUP_ID=-1002481825226

# The link to your group (Username or Invite Link)
# This is used for showing the group link in bot messages.
# Example: https://t.me/moxiclips OR https://t.me/+Abc123xyz
GROUP_LINK=

# The ID of your Admin Log Channel (for tracking searches and errors)
LOG_CHANNEL_ID=-1002612670454

# The Telegram User ID of the Main Administrator 
# Use this to gain access to bot admin commands (/addmovie, /cleanroom, /rooms, etc.)
ADMIN_ID=16476946240

# Webhook URL needed for Koyeb/cloud deployments. 
# Keep it empty to run the bot in 'polling' mode locally on your machine.
WEBHOOK_URL=

# Optionally set a port. Express will use process.env.PORT automatically on Koyeb.
PORT=8000
